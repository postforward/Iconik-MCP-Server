c# Dependencies
node_modules/

# Build output
dist/

# Environment files
.env
.env.local
.env.*.local

# MCP server config (contains auth tokens)
.mcp.json

# Config files with credentials
iconik-config.json
.iconik-config.json

# OS files
.DS_Store
Thumbs.db

# IDE files
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/

# Temporary files
*.tmp
*.temp

# Claude Code project instructions
CLAUDE.md

# Client deployment configs (contain credentials)
deploy/

# Local workflow-specific scripts (not for public repo)
scripts/check-archive-leftovers.ts
scripts/cleanup-trick-after-archive.ts

# TM Trick→Mortar→archive-warm lifecycle scripts (client-specific, not for public repo)
scripts/verify-trick-cloud-parity.ts
scripts/reset-mortar-archive-status.ts
scripts/repoint-trickcloud-to-archive-warm.ts
scripts/expire-mortar.ts
scripts/backfill-mortar-to-warm.ts

# TM lifecycle output/state files (contain client UUIDs + asset data)
parity-report.jsonl
tm-repoint-state.jsonl
tm-expire-state.jsonl
tm-backfill-state.jsonl

# Batch script state files (may contain client-specific UUIDs)
*-state.json
archive-state.json

# Local config files for batch scripts (may contain client-specific UUIDs)
configs/
.backups-do-not-commit

# Warm reclassification — operational runbook + audit dumps (instance IDs, client names)
docs/warm-reclassification-runbook.md
warm-audit/

# Legacy cloud decommission audit — census dumps and reports contain client
# project paths, storage UUIDs and bucket names
legacy/
census/
reports/

# ElevenLabs pipeline runtime dirs
inbox/
backups/
logs/
test/fixtures/*.real.json
